# Real configuration: holds camera credentials and webhook URLs.
# Only config/config.example.json is tracked.
config/config.json
config/*.local.json

# Local assistant working notes, not shipped to contributors.
CLAUDE.md

# Throwaway scripts and probe output. Local only: nothing here is part of the
# project, and some of it touches real cameras.
temp/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Captured data / generated media, in case anyone runs from the repo root
frames/
videos/
logs/
*.log
*.jpg
*.jpeg
*.mkv
*.mp4

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# Built installers (installer/timelapse-maker.iss). Local only: the release
# artifact is the one the Installer workflow builds from a tag.
dist/

# An ffmpeg unpacked by installer/prepare.ps1 when it is run from a checkout.
/ffmpeg/
